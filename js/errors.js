class ERRORS {
  constructor() {
    this.parentIntro = document.getElementById('intro-page');
  }
  errorCategory() {
    //show error if category is not selected
  }
  errorDifficulty() {
    //show error if difficulty is not select
  }
}